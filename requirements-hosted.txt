# Dependencies for the hosted (container) deployment.
#
# Deliberately a standalone list rather than "-r requirements.txt": it adds
# gunicorn (POSIX-only, and the desktop app uses Flask's own server) and the
# object-storage client.
#
# pyodbc is not here on purpose. It needs unixODBC headers and a compiler to
# build, so the Dockerfile installs it in a layer that discards those again -
# listing it here would drag them into the final image.
flask
flask-cors
olefile
openpyxl
gunicorn
google-cloud-storage
